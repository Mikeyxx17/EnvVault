# M1.4 Authentication Adversarial Tests V1

状态：本机自动化子集已落地，不能把本文件当作 M1.4 完成。小时级 fuzz campaign、真实多进程压力、独立人员复核仍缺。

## 已覆盖（自动化）

| 检查 | 测试 | 结果 |
|---|---|---|
| 到期毫秒拒绝；`[issued, expires)` | `credential_expiry_is_registry_enforced_and_rotation_restores_access` | 匹配 verifier 且已到期 → `CredentialExpired`；轮换后恢复 |
| 时钟前跳使凭证失效 | `clock_jump_expires_credentials_and_rollback_cannot_revive_them` | 跳过 expiry 后拒绝 |
| 时钟回拨不能复活 | 同上，以及 expiry 测试中的 `now=50` | last-observed 单调，回拨仍按已观察时间判定 |
| bucket 限流持久化，回拨不能绕过 | `authentication_throttle_persists_and_clock_rollback_cannot_bypass_it` | 5 次失败后正确凭证也被拒；reopen 后仍拒；窗口结束后恢复 |
| 全局限流（50 次跨 bucket） | `throttle::cycling_claims_eventually_blocks_the_global_scope` | 单元层覆盖；不在 Broker 层重复 50 次 Argon2id |
| 并发打开失败关闭 | `concurrent_authentication_throttle_update_fails_closed` | 第二进程 lost-update |

匹配 verifier 且未到期、但处于限流 Blocked 时，对外仍是 `IdentityUnavailable`，不泄露“已过期”。

## 仍缺

- 小时级 / 定时四个 fuzz target campaign 与最小化 corpus 入库。
- 多进程、真实时钟操纵（NTP/VM 时间）压力，而不是测试里传入的 `unix_time_millis`。
- 独立人员按 `independent-review-checklist-v1.md` 复核并关闭 finding。

> 自动化通过不能代替真实操作系统、真实断电或独立安全评审。
